- defaults:
    link:
      create: true
      relink: true

- clean: ['~']
- link:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.ssh/config: ssh/config
    ~/.zshrc
- shell:
    - [git submodule sync --recursive, Synchronizing submodules]
